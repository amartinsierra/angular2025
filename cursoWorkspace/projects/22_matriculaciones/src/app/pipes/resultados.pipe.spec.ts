import { ResultadosPipe } from './resultados.pipe';

describe('ResultadosPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultadosPipe();
    expect(pipe).toBeTruthy();
  });
});
